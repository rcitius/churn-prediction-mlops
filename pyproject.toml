[project]
name = "churn-prediction-mlops"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"